### LangChain – open source development framework for LLM applications.
Python and JavaScript packages
Focused on composition and modularity.

Key value adds:
 - Modular Components.
 - Use cases: Common ways to combine components.

### Components of LangChain:

- Models: - LLMs 20 integrations. Chat models, Text embedding models.
- Prompts: prompt templates, Output parsers, Example selectors.
- Indexes: Document loaders (50+ implementations) Text splitter, Vector Store, Retrievers
- Chains: Prompt + LLMs + Output parsing. Can be used as building blocks, more application specific chains
- Agents: Agent Types. Agent toolkit.
